# 平台隔离方案配套:node 不在 22-26 直接拒绝安装(better-sqlite3 预编译
# ABI 仅覆盖 22/24/25/26,硬性放行会在运行时 ERR_DLOPEN_FAILED)。
# registry 不在此写死,由环境变量 NPM_REGISTRY 控制(默认 npmmirror,见 setup.*)。
engine-strict=true
audit=false
fund=false
