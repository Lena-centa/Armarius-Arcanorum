# Armarius Arcanorum 快速上手指南(零基础版)

本指南面向没有任何编程经验的用户。按顺序做三步:下载 → 部署 → 使用。

---

## 一、需要下载什么?

| 项目 | 必需? | 说明 |
|---|---|---|
| 发布包 `dev_版本号.zip`(约 120MB) | [已完成] 必需 | 包含程序本体 + 完整运行环境,**不需要**再安装任何编程软件(Node.js / Python / 数据库都不用装) |
| 浏览器 | [已完成] 必需 | Chrome、Edge 等现代浏览器(Windows 自带 Edge 即可) |
| ComfyUI(免费开源) | ⬜ 可选 | 仅当你想用"生成"面板或看详情页的节点图时才需要;只浏览、搜索、标注则完全不需要 |
| 网络 | ⬜ 建议 | 首次部署时联网可自动补齐缺失组件;发布包自带运行环境,之后可离线使用 |

> 发布包是单个 zip 文件,从提供方处获取;注意下载**与你的电脑系统匹配**的版本(Windows 版 / Linux 版)。

---

## 二、如何部署(Windows)

### 首次部署

1. **解压**:右键点击发布包 → 全部解压 → 得到程序文件夹。
   建议把文件夹放到一个简单的英文路径,例如 `D:\workflow-archive`(避免中文和空格路径)。
2. **双击程序文件夹里的 `deploy.exe`**:自动完成环境检查、组件补齐、初始化与部署。等待窗口显示部署完成、服务已启动,期间不要关闭窗口。
   > 首次双击若弹出"Windows 已保护你的电脑"提示,点"更多信息"→"仍要运行"(自编译程序无签名,仅首次提示)。
   > 备选:双击无效时,打开 PowerShell 运行 `powershell -ExecutionPolicy Bypass -File .\deploy.ps1`。
3. **打开浏览器**,访问:
   ```
   http://127.0.0.1:8009
   ```
   看到页面即部署成功。

### 日常启停(每次开机)

部署是一次性的;以后开机使用,只需:

- **启动**:双击程序文件夹里的 **`一键启动.bat`**——依赖缺失会自动补齐,启动完成后以**独立应用窗口**打开工具页(由系统默认浏览器提供,Edge/Chrome 为无标签页/地址栏的窗口;Firefox 为新窗口)。控制台窗口保持打开(显示运行日志),不要关闭;也可以双击 `start.exe`,区别仅是普通浏览器页面打开
- **停止**:在启动窗口按 `Ctrl+C`(或直接关闭该窗口);或另开 PowerShell 运行 `.\start.ps1 stop`
- **体检**(排查问题时用):打开 PowerShell 运行 `.\start.ps1 check`

> 若运行脚本时提示"禁止运行脚本",改用完整命令:`powershell -ExecutionPolicy Bypass -File .\start.ps1 start`

---

## 三、如何部署(Linux / WSL)

1. 解压:
   ```
   unzip dev_版本号.zip -d ~/workflow-archive
   ```
2. 进入目录:
   ```
   cd ~/workflow-archive
   ```
3. 一键部署并启动:
   ```
   bash deploy.sh
   ```
   (也可以分开手动执行:`bash start.sh setup` 初始化,再 `bash start.sh start`;
   不带参数直接 `bash start.sh` = 检查 → 缺失自动初始化 → 启动)
4. 浏览器访问 `http://127.0.0.1:8009`

日常启停:`bash start.sh start` / `stop` / `check`。

---

## 四、如何使用

页面顶部的导航可以在几个页面之间切换。

### 首页(记录列表)— 你每天最常看的页面
- 展示全部图片工作流档案(提示词、模型、LoRA、采样器等)。
- **搜索**:顶部搜索框输入关键词(提示词、模型名、LoRA 名等),即时过滤。
- **时间筛选**:选择开始 / 结束日期,只看某段时间的记录。
- **详情**:点击任意一条记录,进入详情页查看完整参数(含节点图;节点图需要 ComfyUI 可达)。

### 生成页(Generate)
基于档案重新生成图片。**需要本机运行 ComfyUI**(可选安装),否则页面会提示不可用。

### 标注页(Labels)
给记录打标签(例如"人像""风景"),之后可按标签筛选。

### 统计页(Stats)
查看数量趋势、分类分布等统计概览。

### 设置页(Settings)— 第一次使用必做一件事
1. 打开设置页,找到**图片目录**。
2. 填入你存放图片/工作流文件的文件夹路径(Windows 示例:`D:\my_images`)。
3. 保存后**重启服务**(停止再启动,见上文"日常启停")。
4. 系统会自动扫描该目录并把记录入库:首页的记录数会逐渐增长。

其余设置(数据库、备份、同步等)保持默认即可,无需改动。

### 推荐的新手流程
设置图片目录 → 重启 → 等扫描完成 → 首页搜索浏览 → 详情查看参数 → 需要时打标签/看统计。

---

## 五、常见问题

| 问题 | 处理 |
|---|---|
| 浏览器打不开页面 | 确认部署/启动窗口还在运行;确认地址是 `http://127.0.0.1:8009` |
| 提示 8009 端口被占用 | 关闭占用该端口的程序后重启;或高级用户在数据目录 `.env` 文件中改 `NEST_GATEWAY_PORT=` 为其他数字后重启 |
| 设置了图片目录但没数据 | 确认目录路径真实存在、无空格问题;保存后必须**重启服务**;大目录扫描需要时间 |
| 提示 ComfyUI 不可达 | 不影响浏览/搜索/标注;只影响"生成"面板与详情页节点图 |
| 数据存在哪、如何备份 | 数据与配置在**用户数据目录**(Windows `%LOCALAPPDATA%\workflow_db`,代码树外,更新/重装不丢失):主库 `gray_workflow.sqlite3` + `.env`。备份 = 先停止服务,再复制该目录;也可在设置页「数据目录」面板点"立即备份" |
| 换电脑迁移 | 停止服务后把整个用户数据目录复制到新电脑同位置,再按"部署"步骤重做一遍即可 |

---

## 关于隐私

默认是**单机运行**:数据只保存在你自己的电脑上,不会上传到任何服务器。
